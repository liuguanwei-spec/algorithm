# 第二章 分治

## 分治法的概念

分治法把一个规模较大的问题分解为若干个较小的同类子问题，分别求解后，再合并各子问题的解得到原问题的解。

### 适合使用分治法的问题

通常具有以下四个特征：

1. **小规模问题容易直接求解。**
2. **原问题可以分解为若干个规模更小、形式相同的子问题。**
3. **各个子问题相互独立。**一个子问题的求解一般不依赖其他子问题的结果。
4. **原问题的解可以由各个子问题的解合并得到。**

> **可分解、同类型、相互独立、可以合并。**

### 分治法的基本过程

$$
\boxed{\text{分解 Divide}\rightarrow\text{求解 Conquer}\rightarrow\text{合并 Combine}}
$$

- **分解**：把原问题划分为若干个较小的同类子问题。
- **求解**：递归求解子问题；规模足够小时直接求解。
- **合并**：将子问题的解组合成原问题的解。

## 分治法的复杂性分析

若一个规模为 $n$ 的问题被分成 $k$ 个规模为 $n/m$ 的子问题，分解与合并的总开销为 $f(n)$，则时间复杂度通常写成：

$$
T(n)=kT(n/m)+f(n)
$$

其中：

- $k$：子问题的个数；
- $n/m$：每个子问题的规模；
- $f(n)$：分解问题和合并结果的开销。

递归方程可用代入法、递归树或主定理求解。

### 主定理

比较 $f(n)$ 与 $n^{\log_m k}$ 的增长速度：

1. 若 $f(n)$ 更慢，则

   $$
   T(n)=\Theta\left(n^{\log_m k}\right)
   $$

2. 若 $f(n)$ 与 $n^{\log_m k}$ 同阶，则

   $$
   T(n)=\Theta\left(n^{\log_m k}\log n\right)
   $$

3. 若 $f(n)$ 更快，并满足正则条件，则

   $$
   T(n)=\Theta(f(n))
   $$

空间复杂度主要由递归调用栈、子问题存储和合并所需的辅助空间决定。

## 递归的基本概念

- **递归算法**：直接或间接调用自身的算法。
- **递归函数**：用函数自身给出定义的函数。
- **直接递归**：函数直接调用自身。
- **间接递归**：函数通过其他函数调用自身。

递归的执行包括两个阶段：

- **递推**：将问题逐步转化为规模更小的同类问题。
- **回归**：到达递归出口后逐层返回结果。

## 递归定义

递归定义由两部分组成：

1. **边界条件**：给出最小规模问题的答案，使递归能够终止。
2. **递归方程**：描述原问题与较小规模同类问题之间的关系。

$$
\boxed{\text{递归定义}=\text{边界条件}+\text{递归方程}}
$$

正确的递归必须有明确的边界条件，并且每次递归都更接近该边界。

阶乘函数：

$$
n!=
\begin{cases}
1, & n=0\\
n(n-1)!, & n>0
\end{cases}
$$

## 递归算法的复杂度

递归算法的时间复杂度通常用递归方程表示：

$$
T(n)=\text{子问题代价}+\text{当前层代价}
$$

常见形式：

$$
T(n)=T(n-1)+O(1)=O(n)
$$

$$
T(n)=T(n/2)+O(1)=O(\log n)
$$

$$
T(n)=2T(n/2)+O(n)=O(n\log n)
$$

递归复杂度可使用代入法、递归树或主定理求解。递归调用的空间复杂度通常与最大递归深度有关。

## 整数划分的递归表示

设 $f(n,m)$ 表示将整数 $n$ 划分为若干个不超过 $m$ 的正整数之和的方案数。

根据是否使用 $m$，有：

$$
f(n,m)=f(n,m-1)+f(n-m,m)
$$

边界条件：

$$
f(0,m)=1
$$

$$
f(n,0)=0\qquad(n>0)
$$

当 $m>n$ 时：

$$
f(n,m)=f(n,n)
$$

普通递归可能重复计算相同子问题；使用记忆化可以避免重复计算。

## 二分搜索

二分搜索每次将问题规模缩小为原来的一半，每层只进行常数次操作，因此：

$$
T(n)=T(n/2)+O(1)
$$

递归展开：

$$
T(n)=T(n/2^k)+kO(1)
$$

当问题规模缩小到 1 时：

$$
\frac{n}{2^k}=1
$$

所以：

$$
k=\log_2 n
$$

因此二分搜索的时间复杂度为：

$$
\boxed{T(n)=O(\log n)}
$$

## 大整数乘法

将两个 $n$ 位整数分别分成高位和低位：

$$
X=A\cdot 10^{n/2}+B
$$

$$
Y=C\cdot 10^{n/2}+D
$$

直接展开：

$$
XY=AC\cdot 10^n+(AD+BC)\cdot 10^{n/2}+BD
$$

直接计算需要进行 4 次规模为 $n/2$ 的乘法：

$$
T(n)=4T(n/2)+O(n)
$$

因此：

$$
T(n)=O(n^2)
$$

利用：

$$
AD+BC=(A-B)(D-C)+AC+BD
$$

只需计算 $AC$、$BD$ 和 $(A-B)(D-C)$ 三次乘法，因此：

$$
T(n)=3T(n/2)+O(n)
$$

根据主定理：

$$
\boxed{T(n)=O\left(n^{\log_2 3}\right)\approx O(n^{1.585})}
$$

## Strassen 矩阵乘法

求两个 $n\times n$ 矩阵 $A$、$B$ 的乘积 $C=AB$。普通矩阵乘法的时间复杂度为 $\Theta(n^3)$。

### 普通分治方法

设 $n$ 为 2 的幂，将矩阵划分为四个 $\frac n2\times\frac n2$ 的子矩阵：

$$
A=\begin{pmatrix}A_{11}&A_{12}\\A_{21}&A_{22}\end{pmatrix},\qquad
B=\begin{pmatrix}B_{11}&B_{12}\\B_{21}&B_{22}\end{pmatrix}
$$

根据分块矩阵乘法：

$$
\begin{aligned}
C_{11}&=A_{11}B_{11}+A_{12}B_{21}\\
C_{12}&=A_{11}B_{12}+A_{12}B_{22}\\
C_{21}&=A_{21}B_{11}+A_{22}B_{21}\\
C_{22}&=A_{21}B_{12}+A_{22}B_{22}
\end{aligned}
$$

需要进行 8 次规模为 $n/2$ 的矩阵乘法，矩阵加法的总开销为 $\Theta(n^2)$，因此：

$$
T(n)=8T(n/2)+\Theta(n^2)=\Theta(n^3)
$$

普通分治并没有降低矩阵乘法的渐近时间复杂度。

### Strassen 的改进

核心思想：**增加矩阵加减法，将每层的子矩阵乘法从 8 次减少到 7 次。**

构造以下 7 个乘积，每个乘积都递归使用 Strassen 算法计算：

$$
\begin{aligned}
M_1&=(A_{11}+A_{22})(B_{11}+B_{22})\\
M_2&=(A_{21}+A_{22})B_{11}\\
M_3&=A_{11}(B_{12}-B_{22})\\
M_4&=A_{22}(B_{21}-B_{11})\\
M_5&=(A_{11}+A_{12})B_{22}\\
M_6&=(A_{21}-A_{11})(B_{11}+B_{12})\\
M_7&=(A_{12}-A_{22})(B_{21}+B_{22})
\end{aligned}
$$

再合并得到结果的四个子矩阵：

$$
\begin{aligned}
C_{11}&=M_1+M_4-M_5+M_7\\
C_{12}&=M_3+M_5\\
C_{21}&=M_2+M_4\\
C_{22}&=M_1-M_2+M_3+M_6
\end{aligned}
$$

最终得到：

$$
C=\begin{pmatrix}C_{11}&C_{12}\\C_{21}&C_{22}\end{pmatrix}
$$

将 $M_1,\ldots,M_7$ 代入并展开，即可验证上述结果与普通分块矩阵乘法一致。展开时必须保持乘法顺序，因为矩阵乘法一般不满足交换律。

### 复杂度分析

每层有 7 个规模为 $n/2$ 的子问题，上述公式共使用 18 次子矩阵加减法，因此当前层的额外开销为 $\Theta(n^2)$：

$$
T(n)=7T(n/2)+\Theta(n^2),\qquad T(1)=\Theta(1)
$$

因为 $n^2$ 比 $n^{\log_2 7}$ 增长更慢，根据主定理：

$$
\boxed{T(n)=\Theta\left(n^{\log_2 7}\right)\approx\Theta(n^{2.807})}
$$

- **递归出口**：当 $n=1$ 时直接进行两个数的乘法；实际实现中通常在矩阵较小时改用普通矩阵乘法。
- **规模处理**：若 $n$ 不是 2 的幂，可补零至不小于 $n$ 的最小的 2 的幂，计算后取结果左上角的 $n\times n$ 部分，渐近复杂度不变。

> **记忆重点：普通分治是 8 次乘法，复杂度为 $\Theta(n^3)$；Strassen 是 7 次乘法，复杂度为 $\Theta(n^{\log_2 7})$。**

## 汉诺塔的时间复杂度

设 $T(n)$ 表示把 $n$ 个圆盘从起始柱移动到目标柱所需的基本移动次数。求解过程为：

1. 将上面的 $n-1$ 个圆盘移动到辅助柱；
2. 将最大的圆盘移动到目标柱；
3. 将辅助柱上的 $n-1$ 个圆盘移动到目标柱。

因此递归表达式为：

$$
T(n)=2T(n-1)+1,\qquad T(1)=1
$$

逐步展开：

$$
\begin{aligned}
T(n)&=2T(n-1)+1\\
&=2[2T(n-2)+1]+1\\
&=2^2T(n-2)+2+1\\
&=2^kT(n-k)+(2^k-1)
\end{aligned}
$$

令 $k=n-1$，并代入 $T(1)=1$：

$$
T(n)=2^{n-1}T(1)+2^{n-1}-1=2^n-1
$$

所以汉诺塔移动次数的精确值和渐近时间复杂度分别为：

$$
\boxed{T(n)=2^n-1},\qquad \boxed{T(n)=\Theta(2^n)}
$$

若将空问题作为递归出口，也可写为：

$$
T(0)=0,\qquad T(n)=2T(n-1)+1
$$

两种边界条件得到的结果相同。

> **记忆重点：两次求解规模为 $n-1$ 的子问题，再移动最大圆盘一次，所以递归式为 $T(n)=2T(n-1)+1$。**

## 后续追加

<!-- 在此处继续添加第二章内容。 -->
