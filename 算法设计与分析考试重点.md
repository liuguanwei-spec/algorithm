# 第一章 算法与复杂性分析

## 算法的基本概念

算法是解决某类问题的一组有限步骤。

### 算法的四个性质

- **有穷性**：执行有限步后结束。
- **确定性**：每一步都有明确含义。
- **可行性**：每一步都能实际执行。
- **输入与输出**：可以有零个或多个输入，但至少有一个输出。

> 部分教材将输入和输出分开，表述为五个性质，以课程教材为准。

### 算法与程序的区别

- 算法是解决问题的方法，不依赖具体编程语言。
- 程序是算法用程序设计语言写成的具体实现。
- **根本区别：算法必须在有限步后终止，程序不一定终止。**

## 算法复杂性分析

算法复杂性描述资源消耗随输入规模增长的变化规律，不表示具体运行时间。

- **时间复杂度**：基本操作执行次数的增长规律。
- **空间复杂度**：额外存储空间的增长规律，通常不包括输入本身。

分析复杂度时，先确定输入规模和基本操作，再统计执行次数，并忽略常数系数与低阶项。

例如：

$$
3n^2+5n+10=\Theta(n^2)
$$

## 渐近复杂度

### 渐近符号

- $O(f(n))$：渐近上界，表示增长速度不超过 $f(n)$ 的常数倍。
- $\Omega(f(n))$：渐近下界，表示增长速度不低于 $f(n)$ 的常数倍。
- $\Theta(f(n))$：渐近紧确界，表示与 $f(n)$ 同阶。
- $o(f(n))$：增长速度严格慢于 $f(n)$。
- $\omega(f(n))$：增长速度严格快于 $f(n)$。

其中：

$$
T(n)=\Theta(f(n))
\iff
T(n)=O(f(n))\text{ 且 }T(n)=\Omega(f(n))
$$

大 $O$ 表示函数的上界，不等同于“最坏情况”；最好、平均、最坏描述输入情况，渐近符号描述函数的增长界限。

### 常见复杂度

$$
O(1)<O(\log n)<O(n)<O(n\log n)<O(n^2)<O(n^3)<O(2^n)<O(n!)
$$

对数的底数通常可以忽略，因为不同常数底数之间只相差常数倍。

### 基本分析规则

- 顺序执行：复杂度相加后保留最高阶。
- 嵌套结构：根据各层实际执行次数相乘。
- 条件分支：分析最坏情况时取代价最大的分支。
- 规模每次减半的循环通常为 $O(\log n)$。
- 多个独立输入规模应分别保留，如 $O(n+m)$。

## 最好、平均与最坏情况

- **最好情况**：最有利输入下的运行时间。
- **最坏情况**：最不利输入下的运行时间。
- **平均情况**：在给定输入概率分布下的期望运行时间。

平均时间的一般形式为：

$$
T_{\mathrm{avg}}(n)=\sum_{x\in I_n}P(x)T(x)
$$

其中 $I_n$ 是规模为 $n$ 的输入集合，$P(x)$ 是输入 $x$ 出现的概率。

顺序查找的典型结果：

- 目标位于第一个位置：最好情况 $\Theta(1)$。
- 目标位于最后或不存在：最坏情况 $\Theta(n)$。
- 目标等概率出现在各位置：平均情况 $\Theta(n)$。

## 摊还复杂度

摊还分析考察一系列操作的总成本，再将成本分摊到每次操作。它不依赖输入的概率分布。

例如动态数组扩容时，单次扩容可能需要 $O(n)$，但连续追加元素的平均摊还成本为 $O(1)$。

## 计算复杂性基础

多项式时间通常被视为“可有效求解”，指数时间和阶乘时间在规模增大后通常难以接受。

### P、NP、NP-hard 与 NP-complete

- **P**：能够在多项式时间内求解的判定问题集合。
- **NP**：给定候选解后，能够在多项式时间内验证的判定问题集合。
- **NP-hard**：至少与所有 NP 问题一样难，不一定属于 NP，也不一定是判定问题。
- **NP-complete**：既属于 NP，又是 NP-hard 的问题。

关系为：

$$
P\subseteq NP
$$

$$
NP\text{-complete}=NP\cap NP\text{-hard}
$$

$P=NP$ 是否成立目前仍未解决。

### 多项式时间归约

若问题 $A$ 可以在多项式时间内转换为问题 $B$，记作：

$$
A\le_p B
$$

这表示可以借助 $B$ 的解法解决 $A$，因此 $B$ 至少与 $A$ 一样难。

证明问题 $B$ 是 NP 完全问题通常需要：

1. 证明 $B\in NP$；
2. 选择已知 NP 完全问题 $A$，证明 $A\le_p B$。

归约方向是：

$$
\text{已知 NP 完全问题}\longrightarrow\text{待证明问题}
$$

## 后续追加

<!-- 在此处继续添加第一章内容。 -->
